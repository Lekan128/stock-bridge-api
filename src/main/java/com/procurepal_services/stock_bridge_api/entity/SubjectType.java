package com.procurepal_services.stock_bridge_api.entity;

/** Which kind of principal a RefreshToken belongs to - see refresh_tokens.subject_type. */
public enum SubjectType {
    USER,
    SUPER_ADMIN
}
